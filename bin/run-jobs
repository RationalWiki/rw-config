#!/bin/bash

if [ $(id -un) != "www-data" ]; then
	echo "Invalid user name"
	exit 1
fi

for wiki in test35_rationalwiki; do
	flock -n /var/run/lock/mw-job-runner-$wiki \
		php /srv/rw_test35/core/maintenance/runJobs.php --wiki=$wiki >> /var/log/mw/runJobs.log
done

